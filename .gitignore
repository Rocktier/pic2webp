# ─── Rust / Tauri ───
src-tauri/target/
# gen/schemas 是 Tauri 生成的；gen/windows 是 MSIX 打包输入（清单模板 + 自有磁贴），
# 必须进版本库——否则 CI 上打包器找不到 bundle.config.json，会退回默认磁贴（10.1.1.11 被拒的原因）。
src-tauri/gen/*
!src-tauri/gen/windows/
# 本机 cargo 镜像配置（rsproxy 等），不该进版本库
.cargo/

# ─── Node.js ───
node_modules/
dist/

# ─── 系统文件 ───
.DS_Store
Thumbs.db

# ─── IDE ───
.vscode/
.idea/
*.swp
*.swo
*~

# ─── 构建产物 ───
*.dmg
*.msi
*.app
*.log

# ─── AI agent 工作文件（含个人信息，不提交）───
findings.md
task_plan.md
progress.md
.trae/

# ─── 工具临时文件 ───
.playwright-cli/

# ─── 设计/计划文档（工作文件，不进公开仓库）───
docs/
